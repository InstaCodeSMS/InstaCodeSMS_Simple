## 项目身份
SimpleJieMa 是一个基于 Cloudflare Workers 的自动化数字产品转卖平台。核心逻辑：通过 Hono 驱动边缘侧业务，使用 HTMX 实现局部刷新，Supabase 负责持久化，对接上游 API 完成自动化接码/发卡。

AI Agent 工作协议 (核心循环)
任务执行循环 (Plan → Act → Verify → Report)
Plan: 在 <thinking> 块中推演。必须定位目标在 src/domains 的哪个领域，或是否需要新增 src/adapters。

Act: 严格遵守最小化 Diff。在 Cloudflare 环境下，优先考虑性能与包体积。

Verify: 运行 vitest 或使用 wrangler dev 模拟边缘环境验证。

Report: 修改后必须说明：“是否增加了边缘侧延迟？” 或 “是否破坏了 HTMX 的局部响应？”

错误恢复协议
如果 wrangler 报错 Service Worker 限制 → 检查是否误用了 Node.js 原生库。

如果 HTMX 请求返回了完整 HTML 而非 Partial → 立即检查 src/views/partials 逻辑。

项目结构与架构感知 (100分架构地图)
1. 强制目录守则
Cline 必须将代码放入以下指定层级：

src/core/: 环境变量校验 (Zod)、统一错误处理、全局常量。

src/adapters/: 外部防腐层。所有调用 Supabase、上游 API、支付接口的代码必须在此。

src/domains/: 业务核心。每个领域（order, user）包含 schema.ts (Zod), service.ts (逻辑), repo.ts (数据访问)。

src/views/: 渲染层。

layouts/: 整体骨架。

partials/: HTMX 专用。仅返回局部 JSX 片段，严禁包含 <html>/<body>。

src/routes/: 极薄层。仅负责将 web 或 api 请求转发给 service。

2. 边缘计算限制 (Edge Awareness)
禁止使用任何依赖 Node.js 原生 API（如 fs, crypto - 除非使用 node: 兼容前缀且 CF 支持）的库。

异步任务（如发送通知、同步状态）优先使用 ctx.waitUntil 以免阻塞响应。

数据库与类型校验规则 (Supabase & Zod)
1. 唯一事实来源 (Single Source of Truth)
数据库类型: 严禁手动修改 src/types/supabase.ts。必须假设其为脚本自动从 Supabase 生成。

输入校验: 所有的 POST/PUT 请求必须先通过 src/domains/*/schema.ts 中的 Zod 校验。

2. 金额与状态
金额处理: 数据库中使用 numeric/decimal。TS 中使用 bigint 或特定精度处理，严禁在金额计算中使用 number (float)。

RLS 感知: 编写 SQL 或 Supabase 调用时，必须考虑 Row Level Security，确保查询中包含 auth.uid() 过滤。

全局禁令 (违反即重写)安全与架构红线禁止行为正确做法❌ 在 routes/ 里直接调用 supabase✅ 在 domains/*/repo.ts 中封装数据操作❌ 硬编码上游 API 秘钥✅ 通过 c.env.SECRET 读取并经过 core/env.ts 校验❌ 局部刷新请求返回完整页面✅ 返回 views/partials 中的组件❌ 擅自引入重量级 npm 包✅ 优先使用轻量级、Edge-ready 的库❌ 在循环中执行数据库查询 (N+1)✅ 使用 Supabase 的 .in() 或封装 RPC各层级编码策略1. 后端逻辑 (Hono / TypeScript)错误抛出必须通过 core/errors.ts。所有的 API 响应必须具备类型定义，利用 Hono 的 RPC 模式（通过 hc）导出给前端（如果需要）。关键逻辑必须包含中文 Why 注释。2. 前端交互 (HTMX / Alpine.js)DaisyUI 优先: 严禁手写复杂 CSS，优先组合 DaisyUI 类名。局部更新: 必须明确 hx-target 和 hx-swap 的范围。加载反馈: 交互按钮必须包含 htmx-indicator，显示加载状态。国际化: 文案必须通过 i18n 处理，禁止在 JSX 中硬编码中文字符串（除非是纯静态内容）。校验指令 (VERIFICATION)Cline 自检清单[ ] 是否符合 src/domains 模块化规范？[ ] 环境变量是否经过 src/core/env.ts 校验？[ ] 是否在 partials/ 中正确处理了 HTMX 局部刷新？[ ] 数据库字段是否与 src/types/supabase.ts 对齐？[ ] 是否使用了 ctx.waitUntil 处理非阻塞异步任务？变更影响声明模板(要求 Cline 在每次重大修改后输出)Markdown### 🚀 架构变更声明
- **修改领域**: [e.g., Domains/Order]
- **HTMX 影响**: [是否影响了局部刷新流程]
- **Edge 适配**: [是否涉及新的 Cloudflare 资源绑定, 如 KV/D1]
- **DB 迁移**: [是否修改了 Supabase Schema]

1. 核心技术栈 (The Tech Stack)
运行时: Cloudflare Workers (Edge Native)

后端框架: Hono (TypeScript)

前端交互: HTMX (Hypermedia) + Alpine.js (微交互)

UI 引擎: Tailwind CSS +Ark UI + Lucide

数据库/鉴权: Supabase

校验: Zod (必须用于所有输入验证)

2. 目录职责与分层规范 (Layering Rules)
严格按照以下职责边界分配代码，严禁跨层调用逻辑：

🟢 src/domains (业务领域层 - 核心)
职责: 包含最核心的业务逻辑（下单、库存逻辑、验证码核销）。

规范:

*.service.ts: 负责编排业务流程。不直接调用 fetch 或 db。

*.repo.ts: 专职与数据库交互。

*.schema.ts: 定义 Zod 模型，作为该领域所有数据的“唯一事实来源”。

🔵 src/adapters (适配器层 - 防腐带)
职责: 封装第三方服务（支付网关、上游接码平台）。

规范: 必须定义标准的 Interface。如果更换上游商，只需在此处增加实现，严禁修改 domains 层的代码。

🟡 src/views (视图层 - HTMX)
职责: 负责渲染 HTML。

规范:

layouts/: 整体骨架。

pages/: 完整页面。

partials/: 高度重要！ 仅返回局部 HTML 片段供 HTMX 替换（如 hx-swap）。

🔴 src/routes (路由层 - 门卫)
职责: 仅负责分发请求。

规范: 严禁在路由中写业务逻辑（超过 5 行代码通常说明逻辑写错位置了）。路由应该只调用 service 并返回 view。

3. 编程契约 (The Contracts)
3.1 绝对类型安全 (Strict Typing)
禁止使用 any。

禁止手动修改 src/types/supabase.ts，该文件由脚本自动生成。

环境变量必须通过 src/core/env.ts 进行 Zod 校验，使用 c.env.KEY 时需确保类型提示正确。

3.2 错误处理 (Error Handling)
统一使用 src/core/errors.ts 中的 AppError。

API 错误: 返回标准 JSON 错误。

Web 错误: 使用 HTMX 返回带错误的局部提示组件（例如 partials/ErrorMessage.tsx），不要直接抛出 500 页面。

3.3 HTMX 交互准则
触发: 优先使用 hx-post, hx-get, hx-target。

加载状态: 必须使用 DaisyUI 的 loading spinner 配合 htmx-request 类显示加载中状态。

局部刷新: 尽量减少全页刷新，利用 partials/ 实现无缝数据更新。

4. AI 行为禁止项 (Do NOT Do)
禁止在 routes/ 里直接写 supabase.from('...')。

禁止在 domains/ 里直接引入 hono 的 Context 对象（应通过参数传递数据）。

禁止在没有 Zod 校验的情况下处理任何 POST 请求数据。

禁止直接使用内联 CSS 样式，必须使用 Tailwind/DaisyUI 类名。

5. 开发工作流建议 (Workflow)
Schema First: 先定义 Zod Schema。

Repo Second: 编写数据库访问层。

Logic Third: 编写 Service 业务逻辑并运行 vitest。

UI Last: 编写 View 组件并挂载到 Route。

📂 项目目录规范 (Project Structure) - 现阶段 75分实用结构
在创建、重构或移动文件时，必须严格遵守以下结构。严禁在非指定目录下创建业务逻辑。

⚠️ 迁移计划：
- 第一阶段（立即）：创建 core/, constants/, utils/ 目录
- 第二阶段（本周）：将 src/lib/payment/ → src/adapters/payment/，src/lib/upstream.ts → src/adapters/upstream/
- 第三阶段（本月）：完善 domains/payment/, domains/sms/, 添加 routes/web/

```
SimpleFaka/
├── .cursor/                 # 🧠 AI 专属知识库与规则引擎
│   ├── rules/
│   │   ├── htmx-rules.md
│   │   ├── domain-logic.md
│   │   └── tailwind-daisyui.md
│   └── docs/                # 上游 API & 支付网关文档
├── .github/                 # 🤖 自动化工作流
│   └── workflows/
│       ├── deploy.yml
│       └── test.yml         # ✨ 新增：自动化测试
├── .env.example             # ✨ 新增：环境变量模板
├── docs/                    # ✨ 新增：项目文档
│   ├── API.md
│   ├── SETUP.md
│   └── DEPLOYMENT.md
├── scripts/
│   └── generate-types.sh
├── tests/                   # 🛡️ 自动化测试 (Vitest)
│   ├── unit/
│   │   ├── domains/
│   │   └── services/
│   ├── integration/
│   ├── e2e/                 # ✨ 新增：端到端测试
│   ├── mocks/
│   └── fixtures/            # ✨ 新增：测试数据
├── src/
│   ├── core/                # ⚙️ 【基建层】
│   │   ├── env.ts           # Zod 校验环境变量
│   │   ├── errors.ts        # 统一错误定义
│   │   └── logger.ts        # 日志配置
│   ├── constants/           # ✨ 新增【常量层】
│   │   ├── api.ts           # API 常量
│   │   ├── errors.ts        # 错误码
│   │   └── business.ts      # 业务常量
│   ├── types/               # 🏷️ 【全局类型约定】
│   │   ├── supabase.ts      # ⚠️ 机器生成 (禁止手动修改)
│   │   ├── api.ts
│   │   ├── env.d.ts
│   │   └── index.ts         # ✨ 新增：类型导出
│   ├── utils/               # ✨ 新增【工具函数层】
│   │   ├── validators.ts    # 验证函数
│   │   ├── formatters.ts    # 格式化函数
│   │   └── crypto.ts        # 加密工具
│   ├── middleware/          # 🛡️ 中间件
│   │   ├── logger.ts
│   │   ├── errorHandler.ts
│   │   ├── csrf.ts
│   │   └── auth.ts          # ✨ 新增（预留）
│   ├── adapters/            # 🔌 【防腐层】
│   │   ├── database/
│   │   │   └── supabase.ts
│   │   ├── payment/         # ✨ 从 lib/ 迁移
│   │   │   ├── index.ts
│   │   │   ├── types.ts
│   │   │   ├── alimpay/
│   │   │   │   ├── client.ts
│   │   │   │   └── types.ts
│   │   │   └── bepusdt/
│   │   │       ├── client.ts
│   │   │       ├── signer.ts
│   │   │       └── types.ts
│   │   └── upstream/        # ✨ 从 lib/ 迁移
│   │       └── client.ts
│   ├── domains/             # 🏢 【领域层】
│   │   ├── order/
│   │   │   ├── order.schema.ts
│   │   │   ├── order.repo.ts
│   │   │   └── order.service.ts
│   │   ├── product/
│   │   │   ├── product.schema.ts
│   │   │   ├── product.repo.ts
│   │   │   └── product.service.ts
│   │   ├── payment/         # ✨ 新增：支付领域
│   │   │   ├── payment.schema.ts
│   │   │   └── payment.service.ts
│   │   └── sms/             # ✨ 新增：短信领域
│   │       ├── sms.schema.ts
│   │       └── sms.service.ts
│   ├── routes/              # 🛣️ 【路由分发层】
│   │   ├── api/
│   │   │   ├── orders.ts
│   │   │   ├── sms.ts
│   │   │   ├── sync.ts
│   │   │   ├── services.ts
│   │   │   └── payment.ts
│   │   └── web/             # ✨ 新增：Web 路由
│   │       └── pages.ts
│   ├── views/               # 🎨 【视图层】
│   │   ├── layouts/         # ✨ 新增
│   │   │   └── BaseLayout.tsx
│   │   ├── pages/
│   │   │   ├── ReceivePage.tsx
│   │   │   ├── PurchasePage.tsx
│   │   │   ├── CheckoutPage.tsx
│   │   │   └── SuccessPage.tsx
│   │   ├── components/      # ✨ 从根目录移过来
│   │   │   ├── Header.tsx
│   │   │   └── Footer.tsx
│   │   └── partials/        # ✨ 新增：HTMX 局部片段
│   │       └── OrderList.tsx
│   ├── app.ts               # Hono 实例装配
│   └── index.ts             # Worker 入口
├── public/                  # 🖼️ 静态资源
├── supabase/
│   ├── migrations/
│   ├── seed.sql
│   └── config.toml
├── wrangler.toml
├── tsconfig.json
├── package.json
├── README.md                # ✨ 新增
└── .env.example             # ✨ 新增
```

**关键改进说明：**

1. **src/core/** - 基础设施集中管理
   - env.ts: 环境变量 Zod 校验
   - errors.ts: 统一错误定义
   - logger.ts: 日志配置

2. **src/constants/** - 常量集中管理（新增）
   - api.ts: API 端点、超时等
   - errors.ts: 错误码定义
   - business.ts: 业务常量

3. **src/utils/** - 工具函数复用（新增）
   - validators.ts: 通用验证函数
   - formatters.ts: 格式化函数
   - crypto.ts: 加密工具

4. **src/adapters/** - 防腐层重构
   - payment/: 从 src/lib/payment/ 迁移
   - upstream/: 从 src/lib/upstream.ts 迁移
   - 删除 src/lib/ 目录

5. **src/domains/** - 领域层完善
   - payment/: 支付业务领域（新增）
   - sms/: 短信业务领域（新增）
   - order/: 添加 order.service.ts

6. **src/views/** - 视图层细分
   - layouts/: 页面布局（新增）
   - components/: 从根目录移过来
   - partials/: HTMX 局部片段（新增）

7. **src/routes/web/** - Web 路由（新增）
   - pages.ts: 页面路由集中管理

8. **tests/** - 测试结构完善
   - e2e/: 端到端测试（新增）
   - fixtures/: 测试数据（新增）

9. **docs/** - 文档目录（新增）
   - API.md: API 文档
   - SETUP.md: 开发环境搭建
   - DEPLOYMENT.md: 部署指南

10. **.env.example** - 环境变量模板（新增）
Read Before Write (先读后写)：在修改任何已有文件前，必须先 read_file 完整阅读内容，严禁基于假设覆盖逻辑。

Safety First (安全第一)：

严禁删除 src/ 目录下未在计划中说明的文件。

在进行大规模重构（移动文件）前，必须输出一份 Moving Plan 列表并请求用户确认。

Refactor Protocol (重构协议)：

如果发现已有代码不符合 domains 或 adapters 规范，应将其迁移至正确位置。

迁移后必须自动修复所有 import 路径，并确保 wrangler dev 能够正常启动。

No Logic in Routes (路由禁令)：

路由层代码严禁超过 10 行。复杂的判断、DB 查询、API 调用必须封装进 domains/[feature]/*.service.ts。